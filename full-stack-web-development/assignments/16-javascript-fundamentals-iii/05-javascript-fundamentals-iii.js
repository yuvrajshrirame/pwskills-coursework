// Question-05: Write a JavaScript program that calculates the area of a circle using the Math object's properties and methods.

const radiusOfCircle = 2;

const areaOfCircle = Math.PI * Math.pow(radiusOfCircle, 2);

console.log(`Area of Circle: ${areaOfCircle}`);